// export const getPrimeIndicatorColor = (age, thresholds) => {
//     const colorKeys = Object.keys(thresholds);
//     for (const key of colorKeys) {
//       if (age < thresholds[key]) {
//         return { color: key };
//       }
//     }
//     return { color: "white" };
// };